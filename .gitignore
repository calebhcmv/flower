# Cloudflare Pages / Wrangler local files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Keep local asset scratch files out of git, but allow deployed site images.
/assets/*
!/assets/img/
!/assets/img/**
